import React from 'react';

const RowContainer = () => {
    return (
        <div className={`w-full my-12 ${flag ? "overflow-x-scrool"}`}>
            
        </div>
    );
};

export default RowContainer;