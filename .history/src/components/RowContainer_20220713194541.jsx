import React from 'react';

const RowContainer = ({f}) => {
    return (
        <div className={`w-full my-12 ${flag ? "overflow-x-scrool" : "overflow-x-hidden"}`}>
            
        </div>
    );
};

export default RowContainer;